# cursor

Small utility for extracting TODO comments from files.

## Usage

python todo_parser.py path/to/file

The parser looks for TODO markers in comment-only lines that start with #, //, --, or ;.
