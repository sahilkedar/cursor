# IMDb-style HTML app

This repository now includes a lightweight IMDb-inspired movie web app built with plain HTML, CSS, and JavaScript.

## Run locally

Open `index.html` in any browser.

Optional (served over local HTTP):

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
